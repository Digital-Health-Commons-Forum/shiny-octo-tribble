# Developers note pad

## Build the image

This assumes you have already built the base, if not read the readme.md in the 
components directory.

To build the core simply issue the following command:

```bash
docker build -t mojocore:service-minio-dev .
```
